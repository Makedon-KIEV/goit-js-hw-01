// Объяви следующие переменные используя ключевое слово const или let и присвой им соответствующие значения.
// topSpeed - число 160.
// distance - число 617.54.
// login - строка "mango935".
// isOnline - буль true.
// isAdmin - буль false.

const topSpeed = 160;
const distance = 617.54;
const login = 'mango935';
const isOnline = true;
const isAdmin = false;

console.log(`Значение переменной 'topSpeed' это число ${topSpeed}`);
console.log(`Значение переменной 'distance' это число ${distance}`);
console.log(`Значение переменной 'login' это строка ${login}`);
console.log(`Значение переменной 'isOnline' это буль ${isOnline}`);
console.log(`Значение переменной 'isAdmin' это буль ${isAdmin}`);
