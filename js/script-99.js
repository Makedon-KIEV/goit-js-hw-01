function filterArray(numbers, value) {
  // Change code below this line
  let filteredNumbers = [];

  for (let i of numbers) {

    if (numbers > value) {
      filteredNumbers.push(numbers);
    }
  }
  return filteredNumbers;
  // Change code above this line
}веревирпшгпгшпшпшнпгшншагнiuhiugiug